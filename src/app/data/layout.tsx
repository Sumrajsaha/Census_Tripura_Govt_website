export const metadata = {
  title: 'Census Data - Directorate of Census Operations, Tripura',
  description: 'Access demographic data, census tables, and statistics for Tripura state.',
};

export default function DataLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
