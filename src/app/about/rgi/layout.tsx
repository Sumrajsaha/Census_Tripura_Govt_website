export const metadata = {
  title: 'Registrar General of India - Directorate of Census Operations, Tripura',
  description: 'Profile of the Registrar General of India.',
};

export default function RGILayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
