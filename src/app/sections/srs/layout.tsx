export const metadata = {
  title: 'Sample Registration System (SRS) - Directorate of Census Operations, Tripura',
  description: 'Information and data regarding the Sample Registration System (SRS) in Tripura.',
};

export default function SRSLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
